From 42d1098f484e1ac54c69adb2fdd69711e7c23a03 Mon Sep 17 00:00:00 2001
From: "Tobias C. Berner" <tcberner@FreeBSD.org>
Date: Sat, 6 Jul 2019 17:02:38 +0200
Subject: [PATCH] Use QTWEBENGINE_VERSION not QT_VERSION for WebEngine feature
 detection

---
 webengineviewer/src/networkmanager/interceptormanager.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git webengineviewer/src/networkmanager/interceptormanager.cpp webengineviewer/src/networkmanager/interceptormanager.cpp
index 84c1cb81..08632ac3 100644
--- webengineviewer/src/networkmanager/interceptormanager.cpp
+++ webengineviewer/src/networkmanager/interceptormanager.cpp
@@ -47,7 +47,7 @@ InterceptorManager::InterceptorManager(QWebEngineView *webEngine, KActionCollect
     for (WebEngineViewer::NetworkPluginUrlInterceptorInterface *interface : d->mManager->interfaceList()) {
         d->mNetworkUrlInterceptor->addInterceptor(interface);
     }
-#if QT_VERSION < QT_VERSION_CHECK(5, 13, 0)
+#if QTWEBENGINE_VERSION < QT_VERSION_CHECK(5, 13, 0)
     webEngine->page()->profile()->setRequestInterceptor(d->mNetworkUrlInterceptor);
 #else
     webEngine->page()->profile()->setUrlRequestInterceptor(d->mNetworkUrlInterceptor);
-- 
2.22.0

