--- src/3rdparty/chromium/media/BUILD.gn.orig	2024-07-03 01:14:49 UTC
+++ src/3rdparty/chromium/media/BUILD.gn
@@ -98,6 +98,9 @@ config("media_config") {
       defines += [ "DLOPEN_PULSEAUDIO" ]
     }
   }
+  if (use_sndio) {
+    defines += [ "USE_SNDIO" ]
+  }
   if (use_cras) {
     defines += [ "USE_CRAS" ]
   }
