--- src/3rdparty/chromium/services/device/geolocation/BUILD.gn.orig	2018-11-13 18:25:11 UTC
+++ src/3rdparty/chromium/services/device/geolocation/BUILD.gn
@@ -92,7 +92,7 @@ source_set("geolocation") {
   if (is_android) {
     sources -= [ "wifi_data_provider_common.cc" ]
   }
-  if (is_chromeos || (is_linux && !use_dbus)) {
+  if (is_chromeos || (is_linux && !use_dbus) || is_bsd) {
     sources -= [ "wifi_data_provider_linux.cc" ]
   }
   if (is_linux && use_dbus) {
